# Clerk authentication — get your key at https://dashboard.clerk.com (API Keys)
# This is a client-side publishable key (safe to expose in the bundle).
VITE_CLERK_PUBLISHABLE_KEY=pk_test_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Note: user maintenance data is stored in the browser (localStorage) per
# signed-in Clerk user, and mirrored to the Clerk user's metadata when it
# fits Clerk's ~8 KB cap. No Supabase configuration is required anymore.
